打开标尺 Ctrl+R
改变单位 在标尺处右键选择像素单位

步骤：
选择工具（自动选择，图层）
    点击旅游，自动定位到对应图层，将该文字图层隐藏
拉参考线
切片
    文件-存储为web所用格式-导出所有用户切片


步骤：
   选择对应要导出的图层元素
   ctrl键 选中多个图层
   右键 转换为智能对象
   双击刚刚转换成功的智能对象，确定 确定
   保存


Ctrl+D 取消选区


 ps:导出 旅游，机票，酒店背景。 
